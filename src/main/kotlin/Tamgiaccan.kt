fun main(args:Array<String>) {
    var i: Int = 5
    var j: Int
    for (k in 1..10) { // vòng lăp for closed rager
        println(k)
    }
    for(k in 1..9){
        var s=k*k
        println(s)
    }
    for(k in 3..20){
        print(k)
    }
}